# GA4 / Google API credentials — never commit these
ga4-oauth.json
ga4-key.json
client_secret_*.json
token.json
*.token.json

# Print/merch source files — full res unwatermarked, keep local only
photos/clean/
photos/originals/
photos/for-review/

# Photo pipeline approval sheet — regenerated per batch, not a site asset
photos/review-sheet.html
